require 'spec_helper'

describe Fin do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Fin.create!(@valid_attributes)
  end
end
