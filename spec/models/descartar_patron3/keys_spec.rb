require 'spec_helper'

describe DescartarPatron3::Keys do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    DescartarPatron3::Keys.create!(@valid_attributes)
  end
end
