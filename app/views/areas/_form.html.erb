<div id="formulario-areas">
  <% semantic_form_for(@area) do |f| %>
  <div id="area-tabs">
      
    <ul>
      <li><a href="#general">Selección</a></li>
      <li><a href="#encabezado">Encabezado</a></li>
      <li><a href="#excluidas">Excluidas</a></li>
      <li><a href="#fin">Fin</a></li>
    </ul>

    <div id="general">
      <%= render :partial => "areas/general", :locals => {:f => f} %>
    </div>
    <div id="encabezado">
      <%= render :partial => "areas/encabezado", :locals => {:f => f} %>
    </div>
    <div id="excluidas">
      <%= render :partial => "areas/excluidas", :locals => {:f => f} %>
    </div>

    <div id="fin">
      <%= render :partial => "areas/fin", :locals => {:f => f} %>
    </div>
  </div>

  <%= f.submit "Salvar" %>

  <% end %>
</div>
